#pragma once

#include "agent.h"

/// Render the Config tab (left panel): provider/model combos, tool gate table,
/// MCP server management, custom commands, and snippets.
void render_config_tab(PrimaryAgent& tab);
